import { IAddressRepository } from '../interfaces/address_repo.interface';

export const addressRepository: Pick<IAddressRepository, any> = {
  //? NOTE: HERE WE CAN IMPLEMENT THE DECLARED REPO METHODS
};
